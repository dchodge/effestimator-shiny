# R session artefacts
.Rhistory
.RData
.Rproj.user/
*.Rproj

# Stan compiled binaries (OS-specific, do not commit)
stan/eff_est
stan/eff_est_none
stan/*.rds
stan/*.so
stan/*.dylib
stan/*.dll

# Fit outputs (large, generated at runtime)
outputs/

# shinyapps.io deployment bundle (generated by rsconnect)
rsconnect/

# macOS
.DS_Store
